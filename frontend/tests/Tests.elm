module Tests exposing (..)

import Expect exposing (Expectation)
import Test exposing (..)


suite : Test
suite =
    describe "All the interesting logic has been offloaded to python, so there's not much of anything to test here anymore" []
